# Hero Sekawan 04/12/25

## Updates:

- Filters for all pages
- OPJ
- Sales modules (sales, return, delivery, payment)
- Sales dashboard
- Sales excel import
- Auto create product/design/supplier/client/opj when doing excel imports

## Update steps

1. Navigate to the backend folder:
   ```
   cd hero-sekawan/backend
   ```
2. Activate the virtual environment:

   ```
   .venv\Scripts\activate
   ```

3. Update dependencies:

   ```
   pip install -r requirements.txt
   ```

4. Run database migrations:

   ```
   alembic upgrade head
   ```

5. Run backend:

   ```
   uvicorn main:app --host localhost --reload --port 8000
   ```
