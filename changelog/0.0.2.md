# Hero Sekawan 04/12/25

## Updates:

- RBAC
- Standalone Design import di maste
- Pie chart drilldown fixed
- Added more filters di dashboard
- Update dashboard
- Chart color consistent with legends
- Entry Auditing (soft delete, created_by, created_at, updated_by, updated_at)
- Calculation purchasing inclulde ppn dan pph

## Update steps

1. Navigate to the backend folder:
   ```
   cd hero-sekawan/backend
   ```
2. Activate the virtual environment:

   ```
   .venv\Scripts\activate
   ```

3. Update dependencies:

   ```
   pip install -r requirements.txt
   ```

4. Configure `.env`:

   ```
   POSTGRES_USER={your_username}
   POSTGRES_PASSWORD={your_password}
   POSTGRES_HOST=localhost
   POSTGRES_PORT={defaults to 5432}
   POSTGRES_DB={your_database_name}
   JWT_SECRET_KEY=supersecretkey123
   JWT_ALGORITHM=HS256
   JWT_ACCESS_TOKEN_EXPIRE_SECONDS=300
   ```

5. Run database migrations:

   ```
   alembic upgrade head
   ```

6. Superadmin seeder:

   ```
   cd hero-sekawan/backend/
   python -m app.seeder.user_seeder
   ```

   Login:

   - username: superadmin
   - password: admin123

7. Run backend:

   ```
   uvicorn main:app --host localhost --reload --port 8000
   ```
